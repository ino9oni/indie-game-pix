# Issue: マップモードの背景を map.png に固定する

## 背景
- ランダム背景のままでは仕様と異なるため、マップ専用画像を使う必要がある。

## 要件
- マップモードで `assets/img/background/map.png` を表示するよう修正する。
- 仕様書に反映する。

## 完了条件
- マップモードで背景が map.png に固定される。
- ドキュメントと実装が一致する。
